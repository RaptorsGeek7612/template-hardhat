## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `pnpm run check` passes locally (lint + compile + test)
- [ ] `pnpm exec tsc --noEmit` passes locally
- [ ] Added/updated tests for the change (Solidity `.t.sol` and/or TypeScript `test/*.ts`)
- [ ] Updated README/docs if behavior or usage changed
